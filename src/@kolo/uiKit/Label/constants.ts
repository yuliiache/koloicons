export const LABEL_TEST_ID = 'labelComponent';

export enum LabelTheme {
  DEFAULT = 'default',
  SUCCESS = 'success',
  PRIMARY = 'primary',
  WARNING = 'warning',
}

export enum LabelSize {
  SMALL = 'small',
  LARGE = 'large',
}
